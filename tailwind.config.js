/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{js,jsx,ts,tsx}',  // Add this to include all the JS, JSX, TS, and TSX files inside src folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
